


```sh
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

```sh

python3 manage.py migrate
python3 manage.py createsuperuser
python3 manage.py runserver
```

```sh
python3 manage.py makemigrations
python3 manage.py migrate
```

```sh
python3 manage.py shell
```

```sh
python3 manage.py test
```

```sh
python3 manage.py test --keepdb
```

```sh
python3 manage.py test --keepdb --failfast
```

```sh
python3 manage.py test --keepdb --failfast --verbosity=2
```

```sh
python3 manage.py test --keepdb --failfast --verbosity=2 --settings=config.settings.test
```

```sh
python3 manage.py test --keepdb --failfast --verbosity=2 --settings=config.settings.test --tag=slow
```

```sh
python3 manage.py test --keepdb --failfast --verbosity=2 --settings=config.settings.test --tag=slow --exclude-tag=fast
```

```sh
python3 manage.py test --keepdb --failfast --verbosity=2 --settings=config.settings.test